const Discord = require("discord.js");
const SQLite = require("better-sqlite3");

module.exports = (client) => {

    client.viewShop = (member) => {

    }

    client.buyItem = (member) => {
        const user = client.get
    }

    client.sellItem = (member) => {

    }
}